cmake_minimum_required(VERSION 3.10)
project(rm_project)
add_executable(rm_project rm.c)
set( EXECUTABLE_OUTPUT_PATH ${CMAKE_SOURCE_DIR}/bin )
install(TARGETS rm_project DESTINATION "${EXECUTABLE_OUTPUT_PATH}")
